-- Deprecated: Use `cumulus.util.engine` instead.
-- This module forwards all calls to `cumulus.util.engine` for backwards compatibility until Rust is fully purged.

return require("cumulus.util.engine")
