---
scope: scala-graalvm-engine-migration
purpose: replace-rust-engine-with-scala-graalvm
altitude: system
updated: 2026-08-12T22:02
---

- (constraint) Remove all Rust code (crates/cumulus-core) and dependencies
- (constraint) Preserve SPEC-031 JSON envelope schema: { success: Boolean, data: Option[T], error: Option[String], error_code: Option[String] }
- (decision) Replace Rust engine with Scala AOT compiled via GraalVM Native Image
- (decision) AD-1: Use Scala 3.4+ as the language for cumulus-engine to leverage clean syntax and compile-time macros
- (decision) AD-2: Use uPickle for compile-time zero-reflection JSON serialization to maximize GraalVM Native Image startup speed and eliminate reflection-config overhead
- (decision) AD-3: Use Scala direct Pattern Matching on args: Array[String] for zero-reflection CLI routing
- (decision) AD-4: Use sbt-native-packager GraalVMNativeImagePlugin for native binary compilation (sbt graalvm-native-image:packageBin)
- (event) spine finalized
